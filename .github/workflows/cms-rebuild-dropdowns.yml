name: "LEGACY - 旧選択肢再生成（使用しない）"

# 旧版CMSを安全に無効化するための互換Workflowです。
# 手動実行してもファイルやサイトは変更しません。
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow disabled
        run: |
          echo "このWorkflowは旧版のため無効化されています。"
          echo "現在はCMS 00〜07を使用してください。"
